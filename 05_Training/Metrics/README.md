# 05_Training/Metrics

Training/validation metrics and benchmark comparisons.

Current Stage 8 artifact:

- `phase_d_supervised_baseline_v907100.json`

The same evaluation records are also registered in
`CBEA_Models.db.ModelEvaluations`.
