# 05_Training/FeatureVectors

Materialized feature vectors.

Current Stage 8 artifact:

- `feature_vectors_v907100.csv`

The file uses the official 22-feature `SFeatureSnapshot` order and is registered
in `CBEA_Models.db.FeatureVectorContracts`.
