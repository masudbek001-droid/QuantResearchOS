# Gateway — Wrapper (Stage 1)

Real implementation lives at `ControlCenter/Gateway/` — see `ControlCenter/Gateway/README.md` and `ControlCenter/docs/OPENAI_GATEWAY_DESIGN.md`.

Stage 1: OpenAI routing stub with `SYSTEM_PROMPT_STAGE1` and `TOOLS_DESIGN`, no OpenAI calls.
