# 02_Databases/Research — Research.db — versioned research schema v11

Runtime file: MT5 `MQL5\Files\CBEA\candlebreakout_<magic>.db`. Additive migration chain 1→11: snapshots(v2), trades(v3), observations(v4), labels(v5), datasets(v6), quality(v7), feature registry(v8), replay(v9), experiments/benchmarks/walk-forward(v10), DataSources+ImportHistory(v11). Owner: `EAData/DatabaseManager.mqh`.

No database file is stored in the repository: all four are created at runtime by the EA.
Contracts are defined in source (`EAData/`, `EAHistory/`) and in `03_Documents/`.
