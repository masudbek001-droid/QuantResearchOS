# GithubWatcher — Wrapper (Stage 1)

Real implementation lives at `ControlCenter/GithubWatcher/` — see `ControlCenter/GithubWatcher/README.md` and `ControlCenter/docs/GITHUB_WEBHOOK_DESIGN.md`.

Stage 1: HMAC verification stub (`verify_signature`), no forwarding.
